project(':collections') {
    dependencies {
        compile project(':typeinfo')
    }
}

project(':hiding') {
    dependencies {
        compile project(':com')
    }
}

configure(subprojects - project(':onjava')) {
    dependencies {
        compile project(':onjava')
    }
}
