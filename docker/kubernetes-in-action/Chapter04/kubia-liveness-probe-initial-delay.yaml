apiVersion: v1
kind: Pod
metadata:
  name: kubia-liveness-probe-initial-delay
spec:
  containers:
  - image: plding/kubia-unhealthy
    name: kubia
    livenessProbe:
      httpGet:
        path: /
        port: 8080
      initialDelaySeconds: 15
